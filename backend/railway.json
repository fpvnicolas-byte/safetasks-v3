{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "/bin/sh -c \"if [ \\\"${RUN_MIGRATIONS_ON_STARTUP:-0}\\\" = \\\"1\\\" ]; then echo 'Running migrations...' && alembic upgrade head || echo 'Migration failed, continuing startup'; fi; python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT\""
    }
}
