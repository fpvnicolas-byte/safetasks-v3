"""Draft API tests placeholder.

This file intentionally stays skipped until shared API fixtures
(`mock_httpx_post`, `db_session`, auth headers, TestClient) are added.
"""

import pytest

pytestmark = pytest.mark.skip(reason="Draft test file: missing shared API fixtures")


def test_draft_placeholder():
    """Keep module discoverable without failing collection."""
    assert True
