# ==========================================
# SAFE TASKS V3 - ENVIRONMENT CONFIGURATION
# ==========================================

# Application Configuration
PROJECT_NAME=Safe Tasks V3
API_V1_STR=/api/v1
ENVIRONMENT=development  # development, staging, production
SECRET_KEY=your-super-secret-key-change-in-production

# Frontend URL (for CORS configuration)
FRONTEND_URL=http://localhost:3000
# Additional CORS origins (comma-separated, no spaces)
# Example (production): BACKEND_CORS_ORIGINS=https://safetasks.vercel.app,https://safetasks-git-main-xyz.vercel.app
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==========================================
# SUPABASE CONFIGURATION
# ==========================================

# Supabase Project Settings
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-supabase-service-role-key-here
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-here

# PostgreSQL Database (from Supabase > Settings > Database)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-postgres-password-here
POSTGRES_SERVER=db.your-project-ref.supabase.co
POSTGRES_PORT=5432
POSTGRES_DB=postgres

# ==========================================
# EXTERNAL API KEYS
# ==========================================

# Google Gemini (for AI script analysis and automation)
# Currently using: gemini-2.0-flash model
GEMINI_API_KEY=your-gemini-api-key-here

# Google Drive Service Account (for cloud sync)
# Path to the service account JSON key file (relative to project root)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
GOOGLE_DRIVE_ROOT_FOLDER_ID=your-google-drive-root-folder-id

# Fiscal Provider (for NF-e integration - e-Notas, FocusNFE, etc.)
FISCAL_PROVIDER_API_KEY=your-fiscal-provider-api-key-here

# ==========================================
# OPTIONAL SERVICES
# ==========================================

# Redis (for rate limiting and caching - optional)
REDIS_URL=redis://localhost:6379

# Email Service (for notifications - optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# WhatsApp (Evolution API - optional, for proposal sending)
# Get from: https://doc.evolution-api.com/
WHATSAPP_API_URL=
WHATSAPP_API_KEY=
WHATSAPP_INSTANCE_NAME=

# ==========================================
# DEVELOPMENT SETTINGS
# ==========================================

# Database URL override (for local development with docker-compose)
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/safetasks_v3

# Debug mode
DEBUG=true

# ==========================================
# PRODUCTION SECURITY
# ==========================================

# Rate limiting settings
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST_SIZE=10

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
#
# 1. Supabase Setup:
#    - Create project at https://supabase.com
#    - Copy URL and service role key from Project Settings > API
#    - Copy JWT secret from Project Settings > API
#    - Copy database credentials from Project Settings > Database
#
# 2. Google Drive Setup:
#    - Create Google Cloud Project
#    - Enable Google Drive API
#    - Create Service Account with Drive access
#    - Download JSON key file and place it at ./service-account.json (relative to project root)
#
# 3. Google Gemini Setup:
#    - Create project at https://makersuite.google.com/app/apikey
#    - Generate API key for Gemini AI
#
# 4. Security:
#    - Change SECRET_KEY to a random 32-character string
#    - Never commit real credentials to version control
#    - Use environment-specific .env files
#
# ==========================================
